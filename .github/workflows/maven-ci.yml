name: Java CI with Maven

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: rassa
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=3

    steps:
    - name: ⬇️ Checkout repository
      uses: actions/checkout@v4

    - name: ☕ Set up Java 17
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: 17

    - name: 🧹 Cache Maven dependencies
      uses: actions/cache@v3
      with:
        path: ~/.m2
        key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
        restore-keys: |
          ${{ runner.os }}-maven-

    - name: ✅ Run Checkstyle
      run: mvn checkstyle:check

    - name: 🔨 Build project
      run: mvn clean install -DskipITs

    - name: 🧪 Run Tests (with MySQL)
      env:
        DB_URL: jdbc:mysql://localhost:3306/rassa
        DB_USER: root
        DB_PASS: root
      run: mvn test

    - name: 📦 Package the application
      run: mvn spring-boot:repackage

    - name: 📁 Upload Spring Boot JAR
      uses: actions/upload-artifact@v4
      with:
        name: flight-ticketing-system-jar
        path: target/flight-ticketing-system-*.jar

